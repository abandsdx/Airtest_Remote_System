# Airtest Cloud Server container
FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app.py ./cloud_server/app.py
COPY storage ./cloud_server/storage

EXPOSE 8081

CMD ["uvicorn", "cloud_server.app:app", "--host", "0.0.0.0", "--port", "8081"]
